diff --git a/sgconfig.yml b/sgconfig.yml
index 44bbd8e..e370898 100644
--- a/sgconfig.yml
+++ b/sgconfig.yml
@@ -6,5 +6,6 @@
 # Policy lives ONLY in .rules/rules/*.yml — never in per-rule Python.
 ruleDirs:
   - .rules/rules
+  - .rules/kotlin-splice
 testConfigs:
   - testDir: .rules/rule-tests
