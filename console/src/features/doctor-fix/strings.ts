// Every word this feature prints. S: labels, three words or fewer, sentence case.
export const S = {
  /** The resting key, on a check whose fix the daemon runs itself. */
  run: 'Run fix',
  /** The armed key says what the second press does, per fix the daemon runs (DoctorFix.wire). */
  confirm: { install_all: 'Relink wrappers' } as Readonly<Record<string, string>>,
  /** The armed key for a fix this console has no words for yet. */
  confirmOther: 'Run it now',
} as const;
