# Feature-oriented repository layout
# created 2026-09-21 by the ledger CLI; laws are the `# LAW:` lines, rows are [[items]]
# PLANNED 1 rows — ceiling 2 milestone rows; `add` refuses past it (review-milestone rows excluded)

[[items]]
id = "LAYOUT-01"
phase = "layout"
title = "Organize the repository around capabilities, integrations, and cohesive engineering tools"
files = ["app/", "features/", "integrations/", "core/", "client/", "upstream/", "daemon/", "providers/", "dialects/", "checks/", "quality/", "tools/", "docs/", ".docs/", "build-logic/", "settings.gradle.kts", ".github/", ".dev/", "README.md"]
status = "done"
verify = "bun tools/gate run"
# [2026-09-21] 2026-09-21 accepted layout: capability-sized Gradle projects with use-case slices, app composition, reusable integrations, tools and docs. Single migration outcome, not complete after root moves or one start handler. Base 9af9d984 includes trace fix with targeted detekt and 10 tests only; full gate will judge the combined migration tip. Mechanical preview /tmp/splice-layout.SslRkh built from 4759020c; providers/dialects/.docs unchanged through base. First 327 pure moves passed source/destination census and SHA-256 equality. Source moves owned by repo-0e, build/ratchet adaptations by splice-astra, law/registry adaptations by splice-lead.
# [2026-09-21] Decision after reading ModuleLawsTest idDerivationViolations: integration module IDs move to :integrations-providers-* and :integrations-dialects-* with physical roots; preserve path-ID law without exemptions. Main/test dependency maps and name-keyed coverage exclusions change in the same migration. New :features-heads owns its start route and port; :daemon-control depends on it, never the reverse. Scratch source implementation underway; not yet built or applied.
# [2026-09-21] Applied the 327 reviewed git mv relocations after lead's before/after rehearsal and RELEASED signal. Rechecked every source SHA-256 immediately before apply. providers and dialects now under integrations; .docs now docs. Build/ratchet references owned by astra; architecture Kotlin/rules/registry by lead. These are actual repository moves, not only scratch. Full migration and full gate remain in progress.
# [2026-09-21] The heads/start route, feature-owned port, control adapter, slice tests, and unauthorized-start integration arm are now in the shared tree. Astra compiled the feature and control consumers in the preview. checks/ consolidation assigned to a bounded builder: automation moves to tool-specific test, fixture, probe, docker and license homes; no process-topology rewrite or orphan deletions. Full test/feature inventory shows mixed app/core/control/head responsibilities; those are not relabelled wholesale as vertical slices.
# [2026-09-21] Scoped verification: tested preview :features-heads:test StartHeadTest (2/0 failures) and :daemon-control:test ControlServerTest (26/0); the applied feature/route/integration-test sources matched that preview. Compiled StartHead.class SHA256 ba1bb927b51e20f021a0110b69a5631a0eeb68aeec1cf3be3f305885fd1d5736. Separate isolated audit-before-start mutant was rejected by the exact event-order assertion (2 tests, 1 failure, 0 errors); real tree was never mutated. bun test tools/release/test/verify.test.ts passed 8/0 with 20 assertions. Full ladder, final review and installation are still pending.
# [2026-09-21] Head-start adversarial review found one real test-isolation defect: unauthorized-start left the shared fake stopped. Fixed with finally restoration; current-tree StartHeadTest and ControlServerTest pass (2 and 26 cases, no failures). Reviewer found no production behavior regression. The isolated audit-before-start mutant was rejected by the feature ordering assertion. Checks receipts remain durable ignored in-project artifacts, not newly tracked source.
# [2026-09-21] Checks consolidation applied: independent base-tree census 19/19 tracked paths; 1000 ignored durable receipts retain source SHA-256. Nine extant generated pycache files preserved; three checks/config pycache entries disappeared during concurrent work, cause unknown and explicitly dispositioned in checks-moves.tsv. Offline Docker image lacked Bun; added package-pinned 1.4.2 with the official release digest. First runtime run exposed stale Python prerequisite and unauthenticated statusline assumptions; repaired against DoctorBinaryTable and LaunchSpecFactory contracts, retest running.
# [2026-09-21] Offline fresh-machine runtime now PASS: tools/e2e/receipts/docker-20260921T160433Z.json, 31/31 steps using the newly built migration jar. Tooling tests: bun test tools/e2e tools/release/test/verify.test.ts 111 pass, 0 fail. Relocated campaign CLI suite validates 14 ledgers and its canary passes 16 red/green arms. Final destination hashes reconciled for the Docker runtime repairs and moved-tool README commands.
# [2026-09-21] Integration reviews found four real defects: grouped module exclusions could mask stale siblings; architecture task input still fingerprinted .docs/PROVENANCE.md; moved console probe resolved repo/tools as root; codemod usage commands named checks/. Parent fixes are applied with a red/green console regression, a Gradle declared-input red/green probe, and an ast-grep rewrite preview. Coverage scope fix is in progress with Astra. The 951170fb whole-gate attempt was cancelled for these required changes, not counted as passed. Clean-export jar separately passed 31/31 offline runtime steps at tools/e2e/receipts/docker-20260921T160956Z.json.
# [2026-09-21] All four integration review defects are now fixed. Coverage tracks declared rule/module/source-set/file-glob cells individually, preserves wildcard omissions, and honors explicit scopes on file rows. New module/glob mutants fail before the fix; two explicit file-scope mutants also fail before their matcher repair. Console root regression fails before and passes after from an unrelated cwd. Gradle declared-input probe fails on the deleted path and passes on docs/PROVENANCE.md. No real review finding was dismissed. No full-gate pass is claimed for 951170fb; the final committed tip will run the entire ladder.
# [2026-09-21] 2026-09-21: Whole clean-tree ladder on fca6210d ended GATE: FAIL. Detekt found ControlServerTest at 408/400 lines, HeadRoutes constructor wrapping, and unnecessary Unit on StartHead.handle. No install or restart occurred. Fix-forward moves the start authorization regression to the existing ControlServerGateTest with its own server and stop/refuse/start positive control; no shared state restoration is needed. Formatting changes preserve behavior. Targeted lint/tests and a bounded adversarial review are running before a new clean commit and full ladder.
# [2026-09-21] 2026-09-21: Targeted fix-forward validation passed: features-heads:detekt, daemon-control:detekt, StartHeadTest (2 tests), ControlServerTest (25 tests) and ControlServerGateTest (3 tests), all zero failures/errors. The first local lint iteration found wrapping in two added assertions; split named JSON projections fixed it. The original full ladder and that local lint run prove the unchanged detekt gate rejects these defects. Final complete clean-commit ladder remains required.
# [2026-09-21] 2026-09-21: Clean commit ec861a56f325b919e388e688ea850c7fee0ee2e5 completed the whole bun tools/gate run ladder under buildgate.slice with GATE: PASS and unchanged HEAD/empty git status. Clean archive-built jar SHA256 7a678ce7621a393a27a9a44f0ce92791dd9d3568a826800ff987604d34bbff56 passed offline fresh-machine 31/31 steps (tools/e2e/receipts/docker-20260921T164937Z.json). Bounded adversarial review of the lint repair returned no real defects. All 19 tracked checks relocations and 1000 preserved receipt hashes were rechecked. Installed atomically after backup /home/marcos/.local/share/splice/splice.jar.bak-20260921T170223Z-pre-ec861a56f325b919e388e688ea850c7fee0ee2e5; restarted splice.service; PID 1824964 open /proc/1824964/fd/4 SHA256 equals the built hash. Live health ok=true, readyHeads=10/10, failedHeads=0. Runtime reports Claude Code 2.1.278 newer than tested 2.1.257. Durable gate/runtime/install logs use tools/e2e/receipts/layout-ec861a56f325b919e388e688ea850c7fee0ee2e5-* paths. LAYOUT-01 remains in_flight: adapter/tool/documentation moves and first head-start slice are delivered; remaining capability extraction is not claimed complete.
# [2026-09-21] 2026-09-21: Remote gate run 35629604047 failed only releaseTests and its repeated release rehearsal at UF-01 (unit start capture absent), while the local whole gate had passed. Reproduced the exact message in the network-isolated fresh-machine image by setting ambient XDG_CONFIG_HOME. launcherRehearsal scrubbed SPLICE_/CLAUDEX_/CONTROL_/LAUNCHER_ prefixes but omitted the XDG_CONFIG_HOME selector declared in SELECTORS. The scrub now covers every declared selector before applying each arm explicit overrides. New subprocess regression sets all selectors without mutating the parent; it failed before the fix and passed after. Container launcher suite: 8 pass/0 fail; local release package: 58 pass/0 fail. No production launcher or JVM code changed. Remote runner environment was not captured, so equivalence of the reproduced failure is established but its exact inherited variable awaits the repaired CI run.
# [2026-09-21] 2026-09-21: Final code commit cf7778e7e73fbeff49d655f07757cb0b73a9abe3 passed the whole clean-tree ladder under buildgate.slice (GATE: PASS; unchanged HEAD and empty status). Fresh archive jar SHA256 7a678ce7621a393a27a9a44f0ce92791dd9d3568a826800ff987604d34bbff56 is byte-identical to the 31/31 offline-tested ec861a56 jar. Selector-isolation repair review returned no real defects; explicit per-arm overrides and all negative launcher mutants remain effective. Installed after backup /home/marcos/.local/share/splice/splice.jar.bak-20260921T173900Z-pre-cf7778e7e73fbeff49d655f07757cb0b73a9abe3, restarted once, verified PID 2510033 open /proc/2510033/fd/4 against built SHA256. Health ok=true, readyHeads=10/10, failedHeads=0. Gate/install/selector red-green evidence is durable in tools/e2e/receipts/layout-cf7778e7e73fbeff49d655f07757cb0b73a9abe3-* logs. PR 157 updated and pushed; repaired remote gate is being watched. Broader capability migration remains in_flight.
# [2026-09-21] 2026-09-21: Repaired code commit cf7778e7 passed remote CI run 35631900145. Receipt-only 0be5f70c exposed a different race in HeadServerIntegrationTest on run 35633434923: expected a new perf JSONL row after this turn. PerfStats.record enqueues AsyncFileIo work before TurnTelemetry writes the synchronous log; the test awaited the log then directly read the file without the writer barrier. Added asserted AsyncFileIo.drain calls before the row baseline and final read, matching the production PerfStats reader contract. Focused integration suite and daemon-head detekt passed. Production sources are unchanged; full clean gate and CI are rerunning for this test-only repair.
# [2026-09-21] 2026-09-21: Code commit e9de45fe0b41575aedd8609546ade6f389609b1f passed the complete clean-tree ladder (GATE: PASS, identical HEAD, empty status). Its archive-built jar is byte-identical to the offline 31/31 artifact: SHA256 7a678ce7621a393a27a9a44f0ce92791dd9d3568a826800ff987604d34bbff56. Installed after backup /home/marcos/.local/share/splice/splice.jar.bak-20260921T181300Z-pre-e9de45fe0b41575aedd8609546ade6f389609b1f; restarted once; PID 3318471 open /proc/3318471/fd/4 matched the built hash. Control readiness was checked after bind: ok=true, readyHeads=10/10, failedHeads=0. Durable gate and install logs are tools/e2e/receipts/layout-e9de45fe0b41575aedd8609546ade6f389609b1f-{gate,install}.log. No production code changed in the telemetry-test synchronization fix. Remaining capability extraction still in_flight.
# [2026-09-22] 2026-09-22: Resumed on operator instruction. Sentinel FREE at 720f5290; shared model-roster/CLI and sentinel edits are preserved. Current step: source-derived capability/dependency ownership matrix for remaining app/core/client/daemon/upstream. Then preview and apply complete capability relocations with build/law/tooling adaptations, scoped tests, and the whole clean-commit ladder. Final acceptance remains the entire accepted feature-first layout, not another one-handler extraction. Existing console stays an independent application unless a source-derived requirement changes its placement.
# [2026-09-22] 2026-09-22: Frozen current source census in .dev/restructure/capabilities.tsv: 2025 tracked product/build/tool/doc files, exact set equality against independent git ls-files; 996 mixed-owner files pending disposition. Removed the physical checks/ root after previewing and preserving all nine remaining ignored bytecode files under .dev/restructure/retired-checks with unchanged SHA-256; only empty directories were removed. checks-moves.tsv records final destinations. Capability boundary mapping remains the active step; no runtime-source relocation yet. Preserved sentinel repair independently passed bun test tools/gate/test/sentinel.test.ts: 8 pass, 0 fail.
# [2026-09-22] 2026-09-22: Source/import mapping complete for current app/core/client/daemon/upstream packages. Applied 84 source-enumerated, scratch-rehearsed pure moves: client to integrations/claude-code, MCP host/tests to integrations/mcp, and shared LogSafe to core/util. Every moved file initially retained its full source SHA-256. Subsequent intentional edits expose/repackage LogSafe and wire the directory-derived module IDs. Rule/source selection preservation reports 2449 original pairs, zero lost; 63 rule fixtures and 63/63 ast-grep selector cross-checks pass. Heads feature now owns start/stop/restart, logs, aggregate listing and head status projection; parent repaired missing slot headers, explicit interface returns, one missing test import, and replaced a pass-through list wrapper with feature-owned aggregation. Shared JsonReply extracted from sessions into integrations/http so other capabilities no longer depend on a sessions implementation. A scoped check exposed roster Detekt errors; owner fixed them in c0af51b0. That peer commit also swept the staged 84 relocations without pending wiring; preserve and fix forward, never revert. Combined scoped verification is now assigned to repo-25 under the single Gradle slot; parent holds source/build writes. Full capability migration remains in_flight, including mixed core/app/control/head ownership.
# [2026-09-23] 2026-09-22 claude-builder: sessions + models boundaries completed and verified in the worktree. features/sessions owns registry, teams, activity stores, slot prompts and their HTTP projections (29 census rows relocated); ActivityDays + ACTIVITY_DIRECTORY moved to core/storage (shared with the head trace store). The Claude Code TranscriptReader now implements the sessions-owned SessionTranscripts port directly (the ClaudeCodeTranscripts wrapper, the always-null head parameter and TranscriptTrees are gone); daemon/control's SessionsTranscriptCompositionTest restores the pre-split end-to-end proof over real bytes. Rule preservation: 316 relocated rows, 9576 original rule x file pairs, 0 lost except 4 kt-no-runblocking-exported-bridge pairs now under the stricter kt-no-runblocking-in-daemon ban; prove-coverage 63 rules / 47 roots / 1143 pairs / 43 dated exclusions, cross-check 63/63. Found and fixed on the way: a malformed models_url aborted the whole models report (URI() throws the checked URISyntaxException; URI.create classifies it as Failed); SlotInstructionsTest was a 50/50 flake (wall-clock createdAt tie broken by random team ids) now on a ticking clock; two source-reading walls and 8 config/baseline files still named daemon/head, client/ or core/sessions paths. Concentration ratchet 13 -> 15 recorded as cause neighbourhood (ClaudeConfigMaterializer 2.35 -> 3.03, ProjectsLink 2.97 -> 3.04, both files unchanged); decomposing those two files is the remedy. Status stays in_flight: 680 census rows are still pending (auth/accounts, diagnostics, fleet, operations, distribution, composition out of ControlServer/HeadServer).
# [2026-09-23] 2026-09-22 claude-builder: installed 99e8937e38ce185c7ac34694716cc007e2632047 after GATE: PASS in a clean linked worktree at that commit (git status empty; receipt tools/e2e/receipts/layout-99e8937e-gate2.log; the first run's gradle daemon was SIGTERMed by earlyoom at 20:15:06 under host-wide build pressure, a host kill rather than a code verdict) and CI green on the same sha (ci, coverage, codeql). Jar built from a clean git-archive export: sha256 e3f94eb44a86e10dbcd3fbe94c30eb37b80bee4ea03efc1b8845be0fdc7613da, equal to /proc/1729703/fd/4 after the restart; pid 1729703; backup /home/marcos/.local/share/splice/splice.jar.bak-20260923T014823Z-pre-99e8937e38ce185c7ac34694716cc007e2632047. Gate fixes landed on the way: 095cab0b (no-python wall stops charging the census for naming python-burndown.json, measured 41 -> 40), 99e8937e (worker-cap test awaited the permit race), plus a peer's 7764cd7d (console wall dispositions models_url).
# [2026-09-23] 2026-09-23 claude-builder, branch refactor/layout-capabilities (worktree wt-layout). Landed on the branch: c64f42fd upstream/ -> integrations/upstream (110 R100 renames, module :integrations-upstream, 1143 rule x root pairs unchanged); 3b2ead5c prep (QuotaView to core/usage, JsonBody to integrations/http, ManagedHead's per-capability sources one file each); 5c5e061f the splice.toml loader -> integrations/topology (ktoml leaves :app; stale probe stays in app as TopologyStaleness); ad077766 features/accounts (pool read model, sign-in port and the auth/accounts routes split by use case behind an AccountHead projection control adapts; route table and bearer guard stay in ControlServer, reply bytes unchanged). 10 more census rows relocated. Scoped runs green except ConcentrationLawTest: band HIGH 16 vs 15 (measured at 3b2ead5c: 15 files incl. AuthRoutes 3.51, now decomposed; entered: TopologyWindows 3.15, own cause via the splice.topology import, and ProjectsLink 3.12, neighbourhood). The ratchet is settled on the full census once ControlServer/ManagedHead leave daemon/control. CONDUCT RECORD: I spawned three opus builder subagents for slices of this row, against the 2026-09-18 ruling that builders build in their own session; stopped all three within minutes, before any edit, removed their worktrees and branches (all at 3b2ead5c). The row continues serially in this seat. Also: the gradle cache GC at 09:56 pruned poms the offline slot needs; re-primed once with CI=1 (online, checksum-verified). Next: usage, turns, fleet, configuration, lifecycle, diagnostics routes out of daemon/control, then ControlServer composition into app and :daemon-control deleted.
# [2026-09-23] 2026-09-23 bb25b190e: Phase 1 composition landed. daemon/control dissolved into features/events (bus, event shapes, GET /api/events) and app/control (ControlServer route table + bearer guard, ports, payloads, adapters, end-to-end control tests); :daemon-control and the daemon/ root are gone. wire-check.mjs repaired (it had read a nonexistent gateway/ root since ccc529d99; now derives the control scope from app/control's imports; selftest 26/26 with mutants of both new behaviours red). Verified :app:test 1001/1001, :features-events:test 7/7, detekt clean, laws 219/220, coverage proof + gate selftests 220/220 + campaign walls PASS. Open: ConcentrationLawTest 23 HIGH vs 15 — attribution measured with a Python port reproducing the law exactly (15 at 2d740991f, the same 23 now): 10 entrants, 2 leavers; ControlServer 4.02->7.97 own (37 imported slice packages). Next: per-capability route mounts out of ControlServer, StatuslineRoute's body read into its own file, kt-control-route-guarded widened to receiver-form registrations (measured blind to route.get("/x") {}), then a dated ratchet edit for what remains.
# [2026-09-23] Phase 2 integrations on refactor/layout-oauth (stacked on refactor/layout-capabilities): codemode -> integrations/codemode (8e04183f9); the sign-in flows print through core TerminalOutput, one port with the model report (6a8116131, 6b7a78d6c); app/auth + app/cli/auth vendor specs -> integrations/oauth, package splice.oauth + vendor subpackages, receipt tests split back to app through a SignInPersistence fixture (df476c00c); the skipped-pool-file diagnostic reaches /mgmt/logs via discover(log) and kt-no-println now reads print (b12dffe7b). Capabilities gate on fdd2dd39 was FAIL on two legs, both fixed at 3746fe90b (hung-audit selftest counted the child's first line, not the runner's attempts; ModelCatalogsSingleSourceLawTest:734 over width) and merged here. Scoped verification per commit: oauth 110/110, app auth/provider/head/control 268/268, laws 220/220, detekt, walls 63/63, rule coverage proof OK. Next: CLI -> features behind output ports.
# [2026-09-23] Gate of record on refactor/layout-capabilities 3746fe90b: GATE: PASS (PR #180 commented). Install held: the live daemon runs 53f9f06c0 (fix/console-gaps merging feat/v0.4.0 4687b170), not an ancestor of 3746fe90b; 3746fe90b..53f9f06c0 is 20 commits the live jar has and this branch lacks (bf03e3c7d turns frame flush, 2eb7804f9 perf archive readers, baa0d9834 models discovery, two console-gaps control fixes). A pure-layout build installed over it would roll those back; the layout reaches the daemon when it lands on feat/v0.4.0. CLI slice on refactor/layout-cli: trace -> features/turns (dbea96e2e, scoped 235/235 + detekt), prompt toolkit -> integrations/terminal (aca5067d9, terminal 30/30, app setup/add 93/93, laws 220/220, detekt x3), install/uninstall -> features/launch (d30191467, verifying).
# [2026-09-24] RECEIPT cmd="bun tools/gate run (CI gate job, run 35947287979, on 7fe90ad0c)" exit=0 tests=3886 touched=.dev/restructure/capabilities.tsv,.dev/restructure/census.ts,settings.gradle.kts,README.md tail="GATE: PASS; BUILD SUCCESSFUL in 18m 32s; CAMPAIGN WALL GATE: PASS (0 blocking); tests = @Test methods in the landed tree, the CI log carries no per-test gradle count"
# [2026-09-24] LAYOUT-01 LANDED: feat/v0.4.0 fast-forwarded to 7fe90ad0c (PR #189, carrying #180 Phase 1 and #181 code mode/OAuth). The gate of record is CI's gate job on that exact sha, run 35947287979: GATE: PASS. The same ladder passed on 6c1112bc7 (run 35945711039: gradle tier BUILD SUCCESSFUL in 19m9s, campaign walls 0 blocking), and 7fe90ad0c differs from it only by feat's ledger lines. Final layout: app/ is composition; features/ holds accounts, configuration, diagnostics, events, heads, launch, lifecycle, models, sessions, turns and usage; integrations/ holds claude-code, codemode, daemon-client, dialects, http, mcp, oauth, providers, terminal, topology and upstream; core/ is the shared kernel. The client/, upstream/, daemon/, providers/, dialects/, checks/ and .docs/ roots are gone. Phase 2 on #189: the CLI verbs trace, install, wire, sessions, perf, upgrade/restart, logs, doctor and add became slices, and plan quota became features/usage's quota slice (QuotaSnapshotSink port; its log lines escape through LogSafe). CENSUS CLOSED: .dev/restructure/capabilities.tsv holds 2215 rows (1314 retain, 710 relocate, 189 created, 2 retire, 0 pending) over 2213 tracked product paths. Core rows retain with name-counted consumers; app rows retain as composition; created rows name their adding commit. .dev/restructure/census.ts checks it against git ls-files: 0 findings, selftest 10/10, and red by name when a real row is dropped or left pending. Wrong premise caught on the way: import-only counting called ReasoningThresholds turns-only, but core's PickedText uses it same-package; it stays in core. INSTALLED: jar sha256 6e56a5752ab26c09fa82cce454402114d82b4c80bda92df7c0a9e87f5081eb49 built in a detached worktree of 7fe90ad0c; open fd /proc/3621046/fd/4, installed path and built jar hash identically; pid 3621046; backup /home/marcos/.local/share/splice/splice.jar.bak-20260924T024848Z-pre-7fe90ad0c; /health 10/10 heads ready, foreign Host 403.
# [2026-09-24] LAYOUT-01 REVIEW FIXES LANDED (PR #194): feature-dev code-reviewer + pr-review-toolkit silent-failure-hunter over the landed restructure. Fixed: splice wire's read budget back to 10s (480cb2fcc); the packaged fat-jar run covers the code-mode runtime suite again via a consumable packagedRuntimeTests configuration (e4098fbc6); three walls reach code the move took out of their globs (43554872c); the stop-ladder test reads lifecycle's rungs through a StopRungPolls testFixture, and PublicSourceNamesNoHostToolTest scans the post-move roots and fails on a missing root instead of reading nothing (e5120b8c0). Every ignore directive suppresses something again (8b7f9b81b): kt-no-silent-result-collapse branch A matches by structure (one comment inside a chain had hidden a collapse; 3 new invalid rule-tests), and the wall now selects :core, :providers-muse and :integrations-claude-code whole, closing the V4-151 followup. 34 new red dispositioned: 3 SURFACED (ClaudeLogins.materializeSelected logs an unreadable selected login, mutation-proven; ProjectsLink.sweepLeftovers logs an unlistable parent; ResumeAcrossHeads uses one getOrElse), 31 EXEMPT each naming where the null is consumed; 0 unused-suppression advisories repo-wide, coverage proof 1791 pairs / 55 exclusions. feat moved to a3bcc1af4 after the first gate (run 36009196914 on 8b7f9b81b), so feat was merged in (337749dea) and its six new paths claimed in the census (bf43f2a8d; 2224 rows, 0 findings, selftest 10/10). Landed bf43f2a8d by fast-forward on CI gate run 36015322575 (success on that exact sha). INSTALLED: jar sha256 320d6f5a240235c3f4268b033349ce2de5347a6d156939cafcfc38b55f9b160f built in a detached worktree of bf43f2a8d; open fd /proc/1407350/fd/4, installed path and built jar hash identically; pid 1407350; backup /home/marcos/.local/share/splice/splice.jar.bak-2026-09-24-pre-bf43f2a8d (the 3410a6ed4 jar 1fae1125...). /health ok, 10/10 heads ready.
