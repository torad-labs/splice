# Feature-oriented repository layout
# created 2026-09-21 by the ledger CLI; laws are the `# LAW:` lines, rows are [[items]]
# PLANNED 1 rows — ceiling 2 milestone rows; `add` refuses past it (review-milestone rows excluded)

[[items]]
id = "LAYOUT-01"
phase = "layout"
title = "Organize the repository around capabilities, integrations, and cohesive engineering tools"
files = ["app/", "features/", "integrations/", "core/", "client/", "upstream/", "daemon/", "providers/", "dialects/", "checks/", "quality/", "tools/", "docs/", ".docs/", "build-logic/", "settings.gradle.kts", ".github/", ".dev/", "README.md"]
status = "in_flight"
verify = "bun tools/gate run"
# [2026-09-21] 2026-09-21 accepted layout: capability-sized Gradle projects with use-case slices, app composition, reusable integrations, tools and docs. Single migration outcome, not complete after root moves or one start handler. Base 9af9d984 includes trace fix with targeted detekt and 10 tests only; full gate will judge the combined migration tip. Mechanical preview /tmp/splice-layout.SslRkh built from 4759020c; providers/dialects/.docs unchanged through base. First 327 pure moves passed source/destination census and SHA-256 equality. Source moves owned by repo-0e, build/ratchet adaptations by splice-astra, law/registry adaptations by splice-lead.
# [2026-09-21] Decision after reading ModuleLawsTest idDerivationViolations: integration module IDs move to :integrations-providers-* and :integrations-dialects-* with physical roots; preserve path-ID law without exemptions. Main/test dependency maps and name-keyed coverage exclusions change in the same migration. New :features-heads owns its start route and port; :daemon-control depends on it, never the reverse. Scratch source implementation underway; not yet built or applied.
# [2026-09-21] Applied the 327 reviewed git mv relocations after lead's before/after rehearsal and RELEASED signal. Rechecked every source SHA-256 immediately before apply. providers and dialects now under integrations; .docs now docs. Build/ratchet references owned by astra; architecture Kotlin/rules/registry by lead. These are actual repository moves, not only scratch. Full migration and full gate remain in progress.
# [2026-09-21] The heads/start route, feature-owned port, control adapter, slice tests, and unauthorized-start integration arm are now in the shared tree. Astra compiled the feature and control consumers in the preview. checks/ consolidation assigned to a bounded builder: automation moves to tool-specific test, fixture, probe, docker and license homes; no process-topology rewrite or orphan deletions. Full test/feature inventory shows mixed app/core/control/head responsibilities; those are not relabelled wholesale as vertical slices.
# [2026-09-21] Scoped verification: tested preview :features-heads:test StartHeadTest (2/0 failures) and :daemon-control:test ControlServerTest (26/0); the applied feature/route/integration-test sources matched that preview. Compiled StartHead.class SHA256 ba1bb927b51e20f021a0110b69a5631a0eeb68aeec1cf3be3f305885fd1d5736. Separate isolated audit-before-start mutant was rejected by the exact event-order assertion (2 tests, 1 failure, 0 errors); real tree was never mutated. bun test tools/release/test/verify.test.ts passed 8/0 with 20 assertions. Full ladder, final review and installation are still pending.
# [2026-09-21] Head-start adversarial review found one real test-isolation defect: unauthorized-start left the shared fake stopped. Fixed with finally restoration; current-tree StartHeadTest and ControlServerTest pass (2 and 26 cases, no failures). Reviewer found no production behavior regression. The isolated audit-before-start mutant was rejected by the feature ordering assertion. Checks receipts remain durable ignored in-project artifacts, not newly tracked source.
# [2026-09-21] Checks consolidation applied: independent base-tree census 19/19 tracked paths; 1000 ignored durable receipts retain source SHA-256. Nine extant generated pycache files preserved; three checks/config pycache entries disappeared during concurrent work, cause unknown and explicitly dispositioned in checks-moves.tsv. Offline Docker image lacked Bun; added package-pinned 1.4.2 with the official release digest. First runtime run exposed stale Python prerequisite and unauthenticated statusline assumptions; repaired against DoctorBinaryTable and LaunchSpecFactory contracts, retest running.
# [2026-09-21] Offline fresh-machine runtime now PASS: tools/e2e/receipts/docker-20260921T160433Z.json, 31/31 steps using the newly built migration jar. Tooling tests: bun test tools/e2e tools/release/test/verify.test.ts 111 pass, 0 fail. Relocated campaign CLI suite validates 14 ledgers and its canary passes 16 red/green arms. Final destination hashes reconciled for the Docker runtime repairs and moved-tool README commands.
