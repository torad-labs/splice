// RED fixture (.kt.txt so it is NOT part of :fir-checks' own src/test compile). A bare
// `produceOrNull() ?: 0` statement: the call sits under FirElvisExpression's lhs, and the elvis
// value itself flows nowhere — the @MustConsume result is discarded. Must be a COMPILATION_ERROR.
package fixtures.red

import splice.core.annotation.MustConsume

@MustConsume
fun produceOrNull(): Int? = 1

fun caller() {
    produceOrNull() ?: 0
}
