# /code-review round 5 on feat/v0.4.0 tip 6eec16e2 — 83 candidates, 67 CONFIRMED, 8 PLAUSIBLE, 9 REFUTED (verdicts.txt). Tier 1 = correctness, fix now.
T1-01 cliC1        UpgradeWrapper.kt:15        upgrade keeps the live shim when no pristine copy exists (every flat 0.3.x install, this box) -> old shim vs new jar version handshake, no claudex launch after upgrade
T1-02 cliC2+cliA2  UpgradeDaemon.kt:56,60      restart judged by the OLD CLI's GATEWAY_VERSION and by whether the unit FILE names the jar (true for a loaded-but-inactive unit) -> false failure on flat installs, false "restarted" while a squatter serves the old jar
T1-03 mcpB1        McpProcessLauncher.kt:24    hosted MCP child spawned in $HOME with the daemon's env; eligibility ignores implicit cwd/env; hosting is default-on -> servers that worked on 0.3.x break
T1-04 mcpB4        McpSharing.kt:86            the full mgmt bearer is baked into every head's .claude.json; rotation 401s live MCP sessions; key readable via claude mcp get
T1-05 turnB4       LoginHookPending.kt:52      /login hook TERM/KILLs every uid `java -jar JAR login <wrapper>` incl. a terminal `--label work` sign-in mid-consent, then spawns the PRIMARY login
T1-06 turnB1       RateLimitCooldown.kt:121    pooled 429 with Retry-After <= 15 s arms a horizon equal to the sleep floor and returns BACKOFF -> the whole wave re-posts together, N x maxRetries (main's GIVE_UP invariant deleted)
T1-07 mcpB3+mcpB2  ControlServer.kt:170, McpHost.kt:70   closeAll before server.stop with no closed flag -> initialize during shutdown respawns an orphan child; sequential 2 s waits blow the 8-10 s budget
T1-08 accB4+accC6  WITHDRAWN 2026-09-14: same code path as the accepted V4-10 FOLLOW-UP POLICY (20 s local protection, no fabricated reset or eviction without reset evidence; quota polling is the exhaustion signal). Not a new defect; distinct from turnB1's synchronized retry storm, which stands.
T1-09 accB1+accA2  AccountPool.kt:133          credentialPresent frozen at boot, no eviction on 401 / refresh failure / deleted file -> sticky session fails every turn while a healthy backup idles
T1-10 turnA1+turnA2 LocalRuntimeProbe.kt:110, Topology.kt:111   boot probe refuses heads that served on 0.3.x: exact /v1/models id match (qwen3 vs qwen3:latest) and isLocal=true for ANY loopback openai-chat base_url; refusal never names local=false
T1-11 mcpB5+mcpC5  HostedServer.kt:154,211     child initialized ONCE with empty capabilities + HOST_PROTOCOL; cached initResult replayed to every session; roots/list = []; capability-gated tools vanish
T1-12 turnB2+turnB3 AdmissionResponses.kt:59, HeadAdmission.kt:72   all-accounts-exhausted 429 carries no Retry-After (reset is in hand) so the SDK retries blind, and never bumps HeadHealthCounters -> /api/heads + doctor say 0 errors on a head refusing 100%
T1-13 turnA4       SessionProject.kt:35        5 s project-miss cache changes the compaction tail -> replay key differs on client retry -> second upstream compaction
T1-14 cliA1        DoctorLogEvents.kt:12       'turn (compact=|...)' consumes compact=, so every per-turn line in doctor --json --with-logs is the dangling 'turn compact=' with no pairs (compact, model lost); untested shape
T1-15 turnC1       ClientVersionObservationTest.kt:76, ClientVersionSurfaceTest.kt:66   hardcode 'splice 0.3.2'; the 0.4.0 bump reds :gateway:test and :control:test; CONTRIBUTING bump list omits tests
T1-16 cliA4        UpgradeRelease.kt:80        --to v0.3.x: the old jar ignores --json, runs the full TEXT doctor (write probes, gh, daemon calls) then is refused with the false "doctor did not run"
T1-17 cliA3        SessionRegistry.kt:116      liveness ignores the registry's pidDomain/procStart; reusedPid one-sided and false when startedAt absent -> foreign host pid reported LIVE, its /proc environ read as the head
T1-18 mcpA1        HostedServer.kt:165         crash backoff `BACKOFF_BASE_MS shl (loop-2)` overflows Long past loop 53 -> 8 of every 64 crashes respawn immediately
# Tier 2 (confirmed, lower blast radius): accA3 one bad sidecar refuses whole head (fail-closed, pinned); accC4 arm-before-sleep then failFast; accC5 synthesized switch after reset; mcpA3 operator edit drops live sessions/503 until relaunch; mcpA6 evictOne 0-activity victim; mcpC2 session leak w/o DELETE; mcpC6 child stderr discarded; cliC3 rollback NoSuchFile on flat install; cliC4 --to without v prefix; cliC5 concurrent upgrade prunes staging; accB2 relative auth.file refuses head; cliB2 statusline primary-quota fallback; accA6 --label auto receipt; cliB5 no previous on first releases/; turnA3 symlink cwd; turnA5 meta stamped on no-op tail; mcpB6 silent torn ~/.claude.json read; cliB4 drift warning once; cliB1 eager HttpClient; turnC3 file= text frozen at boot with no stale signal; turnC4 api-key unparsed misrefusal
# Plausible: turnC2 wrapper-word matcher; cliA6 quoted-JSON key redaction gap; turnB5 continuation-round wait refusal classified api_error; mcpC5; accB5; simp5; reuse1; eff3
# Cleanup (confirmed, no behavior change): alt1 alt3 alt4 alt6 reuse2-6 simp1-4 simp6 eff1 eff2 eff4-6 turnA6
# Refuted: accB3 accB6 cliB6 mcpC4 mcpA4 alt5 alt2 mcpA5 (+ cliB2 /api/usage half)
